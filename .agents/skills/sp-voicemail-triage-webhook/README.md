# SP Voicemail Triage (Webhook)

Webhook + WAV variant of [`sp-voicemail-triage`](../sp-voicemail-triage/SKILL.md).

| Skill | Trigger |
| --- | --- |
| `sp-voicemail-triage` | Outlook inbox + Freshdesk KSOnboarding batch |
| `sp-voicemail-triage-webhook` | HTTP webhook POST with `.wav` file |

Shared routing, vetting, and callback rules live under
`../sp-voicemail-triage/reference/`.
